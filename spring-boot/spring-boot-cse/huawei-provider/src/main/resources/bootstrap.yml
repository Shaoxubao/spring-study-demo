server:
  port: 8010

spring:
  application:
    name: huawei-provider
  cloud:
    servicecomb:
      discovery:
        enabled: true
        watch: false
        address: http://10.238.49.231:30100
        appName: hauweicloud-application
        serviceName: ${spring.application.name}
        version: 0.0.1
      config:
        enabled: true
        serverType: kie
        serverAddr: http://10.238.49.231:30110
        fileSource: com.baoge.yaml
