    -- 天气数据执行逻辑
    -- 1、获取当天和昨天部分天气数据，每隔一个或两小时跑代码：WeatherDataSchedule
    -- 2、第二天来跑脚本 修复缺失的小时数据sql ——> 数据同步sql
    -- 3、执行下面sql查看weather_data_hour表中各地市昨天数据是否已补全，补全后执行删除weather_data表中昨天的数据
    
    SELECT * from weather_data_hour WHERE DATE_FORMAT(time, '%Y-%m-%d') = '2026-04-20' and city_no = '61401';
    
    -- 各个地市
    SELECT city_name, COUNT(1) from weather_data_hour WHERE DATE_FORMAT(time, '%Y-%m-%d') = '2026-04-20' GROUP BY city_name;
    
    -- UPDATE weather_data_hour set CREATE_TIME = '2026-04-20 16:06:30'
    
    -- 删除weather_data表中昨天的数据
    -- DELETE from weather_data WHERE DATE_FORMAT(time, '%Y-%m-%d') < DATE_FORMAT(CURDATE(), '%Y-%m-%d');
